import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.PizzaPeppe.app',
  appName: 'mi-primera-app',
  webDir: 'dist/mi-primera-app',
  bundledWebRuntime: false
};

export default config;
